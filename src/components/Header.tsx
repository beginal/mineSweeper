import React from 'react';
import styled from 'styled-components';

interface Props {}

const Header = (props: Props) => {
  return <StyleWrap>asd</StyleWrap>;
};

export default Header;

const StyleWrap = styled.div``;
